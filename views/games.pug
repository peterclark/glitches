main.game(v-else-if="authenticated")
  nav.navigation
    a.btn.btn-link(href='/genres') Genres
    button.btn.btn-link.signout-btn(v-on:click='signOut')
      img.rounded(v-bind:src="user.photoURL")
      i.fa.fa-fw.fa-sign-out
      | {{ user.displayName }}

  .container
    .row
      .col-sm-12
        h3.center join a game
    .row
      .col-xs-6.col-sm-4.col-md-3.col-lg-2(v-for="game in games")
        include game.pug
      .col-sm-12.center(v-if="games.length==0")
        | There are currently no active games
    .row
      .col-sm-12
        h3.center create a game
    .row
      .col-sm-6.col-md-4.col-lg-3(v-for="genre in genres")
        include genre.pug